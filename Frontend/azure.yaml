# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: Frontend
metadata:
    template: azd-init@1.5.0
services:
    FunWithFlightsUI:
        project: FunWithFlightsUI
        host: containerapp
        language: ts
        dist: build
